/*
 hello-again-further
 author Heather Johnston
 */

// includes
#include <stdio.h>

int main()
{
    // initialising the name variable and seeting a charachter limit
    char name[100];
    scanf("%s", name);
    // pritint out to name with hello message
    printf("Hello\n%s\n", name);
    return(0);
}